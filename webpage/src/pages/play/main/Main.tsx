import { useTranslation } from "react-i18next";
import { useNavigate } from "react-router-dom";

function Page_Play_Main() {
  const { t, i18n } = useTranslation();
  const navigate = useNavigate();

  return (
    <></>
  );
}

export default Page_Play_Main;
